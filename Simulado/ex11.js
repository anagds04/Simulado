/* Ex 11) Tabuada com For
Considere a variável:
let numero = 7;

Utilize for para exibir a tabuada deste número (de 1 a 10). */

let numero = 7;
for(let i = 1; i <= 10; i++) {
    console.log(`${numero} x ${i} = ${numero * 1}`);

}

/* For (let i= 1; <=10; i++) 
Vai até i = 10;
Aumenta i de 1 em 1. 
console.log(${numero} x $ {i} = $ {numero * i});
Mostra no console o resultado da multiplicação.
Usa template strig para escrver o texto com variáveis (${}) */
